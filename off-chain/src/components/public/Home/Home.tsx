// Home.tsx
import Sidebar from '../Sidebar/Sidebar';
import { useHome } from './useHome';

export default function Home() {
    const { } = useHome();

    return (
        <main >
          
        </main>
    );
}

