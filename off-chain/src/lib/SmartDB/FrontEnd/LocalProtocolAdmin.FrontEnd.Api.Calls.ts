import { BaseFrontEndApiCalls    } from 'smart-db';
import { LocalProtocolAdminEntity } from '../Entities/LocalProtocolAdmin.Entity';

export class LocalProtocolAdminApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = LocalProtocolAdminEntity;

    // #region api

    // #endregion api
}
